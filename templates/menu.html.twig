<div id='menu' class='col-lg-3 col-xl-2 offset-xl-1 sidebar'>

	{% if noMenu is not defined %}
		{{ knp_menu_render('main', { allow_safe_labels: true }) }}

		{% if app.user and is_granted('ROLE_ADMIN') %}
			{{ knp_menu_render('admin', { allow_safe_labels: true }) }}
		{% endif %}
	{% endif %}

</div>
